#!/usr/bin/env bash

POETRY_PATH="${HOME}/.poetry"

if [ ! -d $HOME/.poetry ]; then
    mkdir -p $HOME/.poetry
fi

DATE=$(date +'%Y%m%d-%H%M')
POETRY_PATH="${POETRY_PATH}/${DATE}.md"

$EDITOR ${POETRY_PATH}
